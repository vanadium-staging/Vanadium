From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: quh4gko8 <88831734+quh4gko8@users.noreply.github.com>
Date: Mon, 13 Mar 2023 08:41:43 +0000
Subject: [PATCH] Extend //android_webview/common cc target sources and deps

---
 android_webview/common/BUILD.gn               |  5 ++++
 .../common/common_ext_cc_deps.gni             | 26 +++++++++++++++++++
 .../common/common_ext_cc_sources.gni          | 26 +++++++++++++++++++
 3 files changed, 57 insertions(+)
 create mode 100644 vanadium/chromium_src/android_webview/common/common_ext_cc_deps.gni
 create mode 100644 vanadium/chromium_src/android_webview/common/common_ext_cc_sources.gni

diff --git a/android_webview/common/BUILD.gn b/android_webview/common/BUILD.gn
index 366b04b3548a7..04cc20d2ade84 100644
--- a/android_webview/common/BUILD.gn
+++ b/android_webview/common/BUILD.gn
@@ -64,6 +64,11 @@ source_set("common") {
   ]
 
   public_deps = [ "//url" ]
+
+ import("//vanadium/chromium_src/android_webview/common/common_ext_cc_sources.gni")
+ sources += common_ext_cc_sources
+ import("//vanadium/chromium_src/android_webview/common/common_ext_cc_deps.gni")
+ deps += common_ext_cc_deps
 }
 
 mojom("mojom") {
diff --git a/vanadium/chromium_src/android_webview/common/common_ext_cc_deps.gni b/vanadium/chromium_src/android_webview/common/common_ext_cc_deps.gni
new file mode 100644
index 0000000000000..5e9cbe7f5d556
--- /dev/null
+++ b/vanadium/chromium_src/android_webview/common/common_ext_cc_deps.gni
@@ -0,0 +1,26 @@
+# Copyright 2023-2026 GrapheneOS
+# Use of this source code is governed by a GPL-2.0-only style license that can be
+# found in the LICENSE file.
+
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+common_ext_full_path_cc_deps = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+common_ext_rel_path_cc_deps = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+common_ext_cc_deps = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+common_ext_cc_deps += common_ext_full_path_cc_deps
+foreach(rel_path, common_ext_rel_path_cc_deps) {
+  common_ext_cc_deps += [ rebase_path(rel_path) ]
+}
diff --git a/vanadium/chromium_src/android_webview/common/common_ext_cc_sources.gni b/vanadium/chromium_src/android_webview/common/common_ext_cc_sources.gni
new file mode 100644
index 0000000000000..f065553276ac0
--- /dev/null
+++ b/vanadium/chromium_src/android_webview/common/common_ext_cc_sources.gni
@@ -0,0 +1,26 @@
+# Copyright 2023-2026 GrapheneOS
+# Use of this source code is governed by a GPL-2.0-only style license that can be
+# found in the LICENSE file.
+
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+common_ext_full_path_cc_sources = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+common_ext_rel_path_cc_sources = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+common_ext_cc_sources = [
+]
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+# Three lines of code for minimizing conflicts in rebasing changes
+common_ext_cc_sources += common_ext_full_path_cc_sources
+foreach(rel_path, common_ext_rel_path_cc_sources) {
+  common_ext_cc_sources += [ rebase_path(rel_path) ]
+}
